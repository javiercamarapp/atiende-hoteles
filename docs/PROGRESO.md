# Progreso (se escribe mientras se avanza)

Formato: requisito · acción · archivos/commit · prueba · resultado · siguiente paso.

## 2026-09-05
- R-0 (continuidad) · CronCreate `f24bfd35` (cada 2h :37, sesión) + /loop dinámico (ScheduleWakeup) · docs/operacion-bucle.md · CronList · pendiente evidencia 1ª ejecución · siguiente: despachar Sonnet.
- R-0 (ubicación) · barrido local de "empresas agénticas" · docs/BLOQUEOS.md B-001 · — · NO ENCONTRADA · siguiente: esperar ruta del usuario; trabajo de requisitos sigue.
- R-GOB · extracción 05-Gobierno-y-protocolo (Sonnet #4) · docs/referencia/04-gobierno-y-protocolo.md · lectura completa 24/24 págs · 59 requisitos GOB · siguiente: consolidar en docs/REQUISITOS.md cuando terminen #1-#3.
- R-FRONT-REF · inventario frontend Restaurantes (Sonnet #5) · docs/referencia/05-frontend-restaurantes.md · lectura solo-lectura, sin secretos · stack Vite 8.2.2/React 18.3.1/TS 5.8.3/Tailwind 3.4.17/shadcn; logo SVG inline; AdminDashboard sin mobile real · siguiente: usar como base del scaffold hotelero cuando llegue 07-stack.
- R-BACKOFFICE-REF · patrón Likida (Sonnet #6) · docs/referencia/06-backoffice-agentes-likida.md · solo lectura, copia más reciente verificada 3f98a96 · portables: tools sin datos del modelo, mutex fail-closed, presupuesto, loop-guard+fallback, nunca inventar cifra · siguiente: arquitectura hotelera con 07-stack.
- R-BP/LLM · blueprint + DECISIONLLMHOTELES (Sonnet #1) · docs/referencia/01-blueprint-y-decision-llm.md · 101/101 págs · 204 requisitos; runtime por rol (Sonnet canal, Haiku enrutador, Opus batch nocturno), LLM nunca decide precio/ISH/IVA, aprobación humana en dinero · siguiente: consolidar REQUISITOS.md al terminar #2 y #3.
- R-STACK · viabilidad sin Docker (Sonnet #7) · docs/referencia/07-stack-viabilidad.md · experimentos reales (vitest 5 passed en PGlite; embedded-postgres 18.4 arrancó y demostró concurrencia; Playwright channel chrome pasó E2E) · recomendación: Vite+React+shadcn / Hono+JWT / PGlite tests + embedded-postgres integración / Supabase real = integración pendiente · siguiente: ARQUITECTURA.md cuando llegue H20 (#3).
- R-H01-11 · investigación hotelera H01–H11 (Sonnet #2) · docs/referencia/02-investigacion-H01-H11.md · 263/263 págs · 288 requisitos; críticos: conector PMS r/w, WhatsApp Business como canal universal, identidad/datos personales sin LLM, pagos por link tokenizado + CFDI 4.0, gobierno de acciones con aprobación humana · siguiente: consolidar al llegar #3.
- R-H12-21 · investigación H12–H21 (Sonnet #3) · docs/referencia/03-investigacion-H12-H21.md · 262/262 págs + JSON · H20 fija arquitectura (núcleo compartido con restaurantes, Supabase multi-tenant, pipeline Ingress→RawEvent→Adapter→command bus→Reducer→Outbox) · siguiente: consolidar REQUISITOS.md + ARQUITECTURA.md (Sonnet).
- R-ARQ · ARQUITECTURA.md (Sonnet #9) · docs/ARQUITECTURA.md · ADR-001..010: npm workspaces+Turbo, Vite/React/shadcn con tokens Restaurantes, Postgres SQL versionado (PGlite tests + embedded-postgres integración), Hono+JWT+RLS set_config, 15 entidades, patrón Likida en agentes, 6 integraciones PENDIENTE DE CREDENCIALES · siguiente: ACEPTACION.md al llegar REQUISITOS.md (#8); luego revisión adversarial Fable de ambos.
- B-001 · búsqueda ampliada (Sonnet #11) · docs/evidencia-B-001-busqueda-ampliada.md · sin mención local · bloqueo confirmado, 2 intentos · siguiente: esperar ruta del usuario.
- R-GOB/QA · protocolo de auditoría hotelero (Sonnet #10) · docs/auditoria/RUBROS.md, AUDITOR-PROMPT.md · — · listo para rondas docs/auditoria-N · siguiente: ACEPTACION.md y auditoría-0 de documentos al llegar REQUISITOS.md (#8).
- R-ALL · REQUISITOS.md (Sonnet #8) · docs/REQUISITOS.md (439 líneas) · 270 canónicos trazados a ~517 IDs origen; 5 contradicciones para arquitectura · siguiente: ACEPTACION.md + auditoría-0 de documentos (Sonnet, paralelo).
- R-ALL · ACEPTACION.md (Sonnet #12) · docs/ACEPTACION.md · 270 criterios, 168 verificables sin credenciales / 102 dependientes (pendientes hasta integración real) · siguiente: auditoría-0 (#13) → corrección de hallazgos (Sonnet) → decisión sobre H1.
- AUD-0 · auditoría adversarial de documentos (Sonnet #13) · docs/auditoria-0/documentos.md · Fable verificó los 2 críticos (grep GOB-025=0; ADR-003 sin aprobación) · 2C/7A/7M/4B · siguiente: corrección (#14) → reauditoría de lo corregido.
- AUD-0 · corrección (Sonnet #14) · 18 commits fix(aud-0) + docs/auditoria-0/00-SINTESIS.md · 18/19 arreglados, 1 pendiente (H04-018 página, en referencia) · siguiente: reauditoría (#15) → recalificación → si B-001 sigue abierto, parar bucle y reportar.
- AUD-0 · reauditoría (Sonnet #15) · docs/auditoria-0/reauditoria.md · 17/19 cerrados; 2 ALTOS nuevos + 1 BAJO · siguiente: corrección vuelta 2/3 (#16) → verificación por grep de Fable → parar bucle si B-001 sigue abierto.
- AUD-0 · corrección vuelta 2 (Sonnet #16) · eb39620, ba82077, 871a973, 21b4aac, e4fe2e3 · verificación Fable por grep: 0 rastros de roles viejos en ACEPTACION, 6 ganchos energía/IoT en RUBROS, sin archivo ficticio DECISIONS-HUMANAS, H04-018=p.13, paridad IDs REQ↔ACEPT diff=0 · auditoría-0 CERRADA (22 hallazgos: 21 arreglados, 1 propuesto: reauditar RUBROS en la ronda 1).

## ESTADO AL DETENER EL BUCLE (2026-09-05)
**Implementado y verificado (fase documental):** 7 referencias (3.063 líneas, 630 páginas de PDF leídas completas), REQUISITOS.md (276 REQ canónicos, 0 IDs origen sin destino), ACEPTACION.md (276 criterios; 102 dependen de credenciales), ARQUITECTURA.md (ADR-001..011, hitos H1–H11), protocolo de auditoría hotelero, auditoría-0 con corrección en 2 vueltas y recalificación (REQ 9, ARQ 9, RUBROS 7, PROMPT 8, BLOQ 8, BUCLE 7). 45 commits locales.
**Pendiente (no ejecutable sin el usuario):** TODO el código (H1–H11) — el encargo limita el trabajo a requisitos/investigación mientras la carpeta destino no exista (B-001). Decisiones expuestas D-001..D-004.
**Motivo de parada:** bloqueo externo B-001 (2 intentos documentados, búsqueda local agotada) sin trabajo independiente restante. Cron f24bfd35 eliminado; /loop detenido.
**Reanudar:** confirmar ruta destino → `mv atiende-hoteles-staging <ruta>/atiende-hoteles` → `claude --model fable` → `/loop` + "reanuda desde docs/PROGRESO.md; hito H1 de docs/ARQUITECTURA.md; solo agentes model=sonnet".

## 2026-09-06
- Reanudación · usuario: "continua" · B-001 reclasificado a reubicación pendiente; D-001..D-004 con defaults documentados · cron 4fe17d3a · siguiente: H1 (db+scaffold) en main y H3 (frontend) en worktree, ambos Sonnet.
- H3 · Fable MIRÓ el render (login 1280, login Restaurantes, resumen 1280 y 390): paridad real de logo, tipografía (serif de bienvenida, Inter, mono en rótulos), tarjeta azul del login, sidebar con pill activa, pie con tema y usuario, BottomNav móvil; StatCards con guion y explicación (sin ceros falsos) · observaciones del orquestador para H2/H4: (1) el rótulo "Pendiente de credenciales del PMS" en Resumen debe pasar a "Sin conexión con el API" cuando falle el API propio y a datos reales de nuestra BD cuando responda: ocupación/ADR/RevPAR/reservas del día salen de reservation/availability propias, no del PMS; (2) login sin "Continuar con Google" es desvío intencional (JWT propio, ADR-004), anotar en README · merge del worktree pendiente hasta que H1 termine en main.
- ADR-001 · scaffold monorepo: turbo.json, packages/config (tsconfig base + eslint flat), tsconfig/eslint/vitest raíz, scripts npm (lint/typecheck/test/test:unit/test:integration/db:migrate/db:reset) · commit `a2ca779` (`package.json`, `turbo.json`, `packages/config/*`, `eslint.config.js`, `tsconfig.json`, `vitest.config.ts`) · prueba: `npm install` sin error + `npm run lint`/`typecheck` en verde · resultado: OK · siguiente: packages/domain-hotel/agent-core en H2+.
- REQ-TEN-001/002/003, REQ-GOB-011, REQ-REC-004, REQ-GOB-010 (fundamento) · esquema core H1 (org/location/hotel, staff_user/hotel_staff 8 roles, room_type/room/rate_plan/availability+advisory lock, reservation con máquina de estados, guest, folio/charge/payment, audit_log encadenado, outbox, idempotency_key), RLS en toda tabla de tenant, rol `atiende_app` sin BYPASSRLS + `authenticated` por transacción, REVOKE ALL FROM PUBLIC + grants explícitos · commit `9ab95b8` (`packages/db/migrations/0001..0010`, `packages/db/src/*`, `packages/db/README.md`) · prueba: ver siguiente línea (suite de tests) · resultado: migraciones aplican limpio contra PGlite y embedded-postgres · siguiente: housekeeping_task/maintenance_ticket y RPC SECURITY DEFINER anónima quedan para H2/H6 (fuera del listado explícito de H1).
- REQ-TEN-001 (aislamiento org y hotel), REQ-GOB-010 (expand-only/idempotencia del runner), REQ-GOB-011 (numeric(12,2)), REQ-REC-004 (reservation_status_event append-only + reversión de cargos), audit_log (hash encadenado + inmutabilidad), idempotency_key (UNIQUE), advisory lock/concurrencia real, outbox atómico · commit `c0854ae` (`tests/unit/*`, `tests/integration/*`, `tests/support/*`) · prueba: `npm run test:unit` (35 tests) y `npm run test:integration` (3 tests, embedded-postgres real) · resultado: 38/38 verdes, `docs/logs/h1-test-unit-20260906-045521.log` y `docs/logs/h1-test-integration-20260906-045521.log` · siguiente: repetir la matriz de aislamiento por cada tabla de tenant nueva que se agregue en H2+; construir `scripts/checks/migraciones-expand-only.ts` y `scripts/checks/schema-location.ts` (REQ-TEN-002 completo).
- H1 · 4 compuertas de calidad corridas y guardadas · `npm run lint` (`docs/logs/h1-lint-20260906-045521.log`, exit 0), `npm run typecheck` (`docs/logs/h1-typecheck-20260906-045521.log`, exit 0), `npm run test:unit` (35 passed), `npm run test:integration` (3 passed) · resultado: 4/4 en verde, sin maquillaje (ver logs reales) · siguiente: mergear `worktree-agent-ac8ce1392c8c2689c` (H3) a `main` ahora que H1 cerró; empezar H2 (Hono/JWT/backend) sobre este esquema.
- H3 (REQ-UX-001, REQ-UX-002, REQ-UX-003) · frontend Vite+React+TS+Tailwind+react-router v7+TanStack Query v5 en worktree, identidad portada de atiende-restaurantes (`packages/ui`: tokens HSL con dark completo, AtiendeLogo, ThemeSelector, Sidebar hotelero, BottomNav móvil, StatCard/EstadoVacio/EstadoError/EstadoCargando) + `apps/web` (login, shell con 12 rutas hoteleras, selector multi-hotel, cliente `api.ts` tipado, 404/privacidad/términos) · commit `feat(h3): frontend del panel hotelero con identidad de atiende-restaurantes` · `npm run lint`/`typecheck`/`build` en verde (0 errores, 2 warnings menores); Playwright (`channel: chrome`) 13 passed/1 skipped (6 capturas 1280×800+390×844, 6 axe sin violaciones serias/críticas, captura real de login de atiende-restaurantes) · resultado real: pantallas sin backend muestran EstadoError/EstadoVacio honestos (nunca datos simulados), evidencia en `docs/logs/h3-*.log` y `tests/e2e/screenshots/` · siguiente: cuando exista `packages/db`/API real (H1/H2), conectar `VITE_API_URL` y verificar paridad visual completa de las 12 pantallas de ACEPTACION.md §5 (aún pendiente, solo 3 rutas cubiertas en H3).
- MERGE H3→main (Fable) · 2be0486 + 862c863 (lockfile regenerado) · compuertas post-merge: lint 0 err/1 warn, unit 35/35, integración 3/3, build web OK, **typecheck ROJO** (tests/e2e/*.spec.ts: @axe-core/playwright sin firma de constructor, imports sin extensión bajo node16, `window` sin lib DOM) → defecto de integración DEF-001 asignado a H2 paso 0.
- H6a · packages/agent-core (Sonnet #20, worktree) · 0d8528f · 73 pruebas nuevas, suite 108/108, lint 0 · REQ-AGT-001/002/004/006/007/009/020/022, SEG-012/013 · merge pendiente hasta que H2 termine en main; pendiente ApprovalQueue persistente (H6b) y LlmProvider real (H9).
- DEF-001 · aislar `tests/e2e/tsconfig.json` (lib DOM + moduleResolution bundler) del tsconfig raíz (node16), corrige imports sin extensión/`window`/interop `@axe-core/playwright` · commit `fix(def-001): ...` · `npm run typecheck` (0 errores) + `npx playwright test` (13 passed/1 skipped, chrome del sistema) · resultado: OK, ver `docs/logs/def-001-*.log` · de paso: `vitest.config.ts` excluye `.claude/worktrees/**` (el patrón posicional `tests/unit`/`tests/integration` hacía match dentro de los worktrees de otros agentes y duplicaba/flakeaba la ejecución).
- H2 (REQ-TEN-003, REQ-REC-003 parcial, REQ-REC-004 heredado H1, REQ-INT-012 parcial, REQ-INT-014 parcial) · `apps/api` (Hono+TS+Node25): login email/contraseña (`scrypt`, `packages/db/src/password.ts`) + JWT propio (`jose` HS256, access 15min+refresh 30d) + sesión por request vía `withAppSession` real (RLS aplica de verdad) + autorización en 2 capas (middleware `requireHotelMembership`/`assertRole` + RLS de `packages/db`) + rutas `/hoteles`, `/hoteles/:id/resumen` (ocupación/ADR/RevPAR/reservas de hoy desde `availability`/`rate_plan`/`reservation` propias, null honesto sin filas), `/reservas` (list/get/create idempotente con `book_availability` por noche/PATCH transición), `/disponibilidad` (rango, mínimo por tipo de habitación), `/huespedes` (list/get/create), `/folios` (get/cargos/pagos idempotentes) + idempotencia real (`Idempotency-Key` obligatorio, reclamo-antes-de-mutar, mismo cuerpo→misma respuesta, cuerpo distinto→422) + outbox transaccional (`reservation.created`/`payment.recorded` en la misma tx) + worker con backoff exponencial y dead-letter (`drainOutboxOnce`) + rate limit en memoria (IP+usuario, interfaz `RateLimitStore` para Redis) + logger `pino` estructurado + `/health`/`/ready` · migraciones nuevas `packages/db/migrations/0011` (staff_user.password_hash con GRANT por columnas, idempotency_key.request_hash+UPDATE) y `0012` (audit_log.seq, corrige flake real de desempate de hash encadenado detectado en H1) · frontend: `Resumen.tsx` ahora usa `EstadoError` real ("Sin conexión con el API") en vez de "Pendiente de credenciales del PMS", y "Sin datos todavía" por cifra individual (REQ-UX-002); `useHotel.tsx` corregido (bug real encontrado en el E2E: la consulta `["hoteles"]` se disparaba sin token en `/login`, quedaba cacheada en error con `retry:false` y NUNCA se reintentaba tras un login exitoso — ahora `enabled: Boolean(sesion)` + `queryKey` incluye el token) · pruebas nuevas: `tests/integration/api/*.spec.ts` (16), `tests/adversarial/*.spec.ts` (28: matriz completa de 8 roles+escalada, aislamiento tenant/hotel con una ORG ajena real, concurrencia HTTP real última habitación 1×201/1×409, inyección SQL/HTML en campos de texto, rate limit 429), `tests/unit/api/*.spec.ts` (9: outbox backoff/dead-letter, rate limiter), `tests/e2e/login-real-y-resumen.spec.ts` (login real con credenciales de la seed + Resumen con ADR real de la seed, arranca api+web reales) · compuertas: lint 0/0, typecheck 0, test:unit 44/44, test:integration 19/19, test:adversarial 28/28, build OK, e2e 14 passed/2 skipped — logs en `docs/logs/h2-*.log` · pendiente/rojo declarado: RPC `SECURITY DEFINER` anónima (REQ-TEN-004), conector PMS real con `(connector,external_id,external_version)` (REQ-REC-003 completo), webhooks HMAC+dedupe (REQ-INT-014 completo, no hay proveedor externo todavía), Ingress/RawEvent/Adapter/command-bus formal (REQ-INT-012 completo, H9), backlog state machine de agente (REQ-GOB-007) y script de auditoría de comandos destructivos (REQ-GOB-009) — ninguno de estos 2 últimos es parte de `apps/api`; sin connection pooling (nueva conexión Postgres por request, aceptable en esta fase); worker de outbox no corre automáticamente (sin cron todavía).
- H6a (REQ-AGT-001/002/004/006/007/009/018/020/022, REQ-SEG-012/013, ADR-006/007) · `packages/agent-core` en worktree: `defineTool`/`ToolRegistry` (GOB-026 en tiempo de definición: external/money exige needsApproval, alwaysApprove prohibido en precio/emisión, esquema sin org_id/hotel_id/guest_id), `InMemoryApprovalQueue` (idempotencia por tool+hash(input)+hotel, doble confirmación en dinero, expiración, texto exacto), `AgentRunner` (loop-guard antes de gastar la última ronda, presupuesto tokens/ms/USD, fallback de proveedor con atribución de costo correcta, truncamiento tratado como error, `no_configurado` honesto sin credenciales vía `EnvProvider`/`FakeProvider` determinista), `buildContext`/`redact` (REQ-AGT-022/GOB-025: fragmento de otro tenant se rechaza con `CrossTenantContextError`; PII de traza redactada), `roles.ts` (Sonnet 5 canal/Haiku 4.5 enrutador/Opus 5 batch nocturno, override por env, gates shadow→propone→autopilot con default shadow y prueba de que shadow no ejecuta ninguna tool de escritura) · commit pendiente en esta rama · `npm run lint` (`docs/logs/h6a-lint-20260906-052055.log`, exit 0), `npm run typecheck` root (`docs/logs/h6a-typecheck-root-20260906-052055.log`, ROJO solo por DEF-001 preexistente en tests/e2e, fuera del alcance de H6a) + typecheck aislado de agent-core (`docs/logs/h6a-typecheck-agent-core-20260906-052055.log`, exit 0), `npm run test:unit` (`docs/logs/h6a-test-unit-20260906-052055.log`, 108/108 incluyendo 73 nuevos de agent-core en 9 archivos) · siguiente: implementación de ApprovalQueue respaldada por Postgres (tabla agent_task/approval) y adaptador real de LlmProvider cuando haya credenciales (H2+/integraciones).
- H2 · apps/api Hono + JWT + 8 roles + RLS set_config + idempotencia + locks + outbox + rate limit (Sonnet #19) · c64c27b…939e67b · compuertas verdes (unit 44, integ 19, adversarial 28, E2E login real) · REQ-TEN-003/REC-003/INT-012/INT-014 parciales; pendientes REQ-TEN-004, pooling, webhook HMAC.
- MERGE H6a→main (Fable) · 0c7589c · compuertas post-merge: lint 0 err, tsc 0, unit 117/117, integ 19/19, adversarial 28/28, build OK · worktrees fusionados eliminados · snapshot detached en .claude/worktrees/auditoria-1-snapshot @0c7589c para auditoría-1.
- AUD-1 (Fable) · ronda de auditoría de FUNDAMENTOS sobre snapshot 0c7589c con 6 rubros (seguridad, backend, datos, agéntico, tool calling, pruebas); los otros 6 (frontend, fiscal, legal, arquitectura, operabilidad, rendimiento) se auditan en la ronda completa al cerrar H5–H8 porque su código aún no existe. Desvío documentado respecto a los 12 de Likida.
- AUD-1 · 6/6 informes recibidos: seguridad 3, backend 5, datos 3, agéntico 4, tool-calling 3, pruebas 5 (media 3.8) · 10 CRÍTICOS en total, todos reproducidos empíricamente por los auditores · plan de corrección: (a) agent-core ahora (#29, sin colisión); (b) reservas/monto/folio delegados a H4 en curso; (c) db/RLS/audit_log/outbox tras cierre de H4 (migraciones ≥0013).
- H9+H11 · packages/mcp-servers (Sonnet #22, worktree) · a84ae8b · 111 tests, lint 0, tsc 0 · REQ-INT-001/002/003/005, BO-027/028/029, INT-007/008, RES-017, REC-009, SEG-015 (parciales por credenciales) · merge pendiente hasta cierre de H4.
- AUD-1 · corrección packages/agent-core (Sonnet #29) · docs/auditoria-1/correccion-agent-core.md · 16/16 hallazgos verificados y arreglados (2 CRÍTICOS + 3 ALTOS + 2 MEDIOS de tool-calling.md; 1 CRÍTICO + 4 ALTOS + 3 MEDIOS + 1 BAJO de agentico.md), ninguno descartado · 12 commits `fix(aud-1/agent-core): ...` sobre `approval.ts` (idempotencia con ámbito de conversación/actor, `inputSummary` legible para el aprobador, doble confirmación por ROL, estado terminal `accion_rechazada`), `tool.ts` (recorrido estructural completo del esquema: anidado/arreglo/`.passthrough()`/`z.record`/`z.any`, lista de sinónimos configurable), `runner.ts` (loop-guard con ventana de N pasos y firma canónica post-Zod, `run_finished` emitido en las 8 salidas, `disable_parallel_tool_use` anclado en tipo+runtime, presupuesto comprobado también después de cada llamada, disclosure de IA mínimo via `ctx.isFirstTurn`, mensaje de error sin detalle interno, `alwaysApprove` cableado), `redact.ts` (CURP+RFC persona física/moral con variantes espacio/guión), `roles.ts` (`roleParamsForChannel` voz=low) · 40 pruebas nuevas en `tests/unit/agent-core/*.spec.ts`, todas escritas ANTES del fix (reproducían el hallazgo en rojo) · compuertas: `npm run lint` 0 errores, `npm run typecheck` (raíz, incluye tests/unit/agent-core) 0 errores, `npx tsc -p packages/agent-core --noEmit` 0 errores, `npx vitest run tests/unit/agent-core` 113/113 verde — logs en `docs/logs/aud1-agentcore-*-20260906-063634.log` · pendiente documentado (fuera de agent-core, no tocado): identificador de conversación/folio propio en `ToolContext` para el caso residual del CRÍTICO 1 (mismo staff actor en 2 conversaciones simultáneas), disclosure de IA completo (detección real de primer turno por canal + copy legal, vive en session/API), `ApprovalQueue` respaldada por Postgres · siguiente: reauditoría de agent-core cuando corresponda; el resto de correcciones de AUD-1 (seguridad, backend, datos, pruebas) siguen su propio plan.
- H4 (REQ-RES-002 parcial/004/005/007/REV-001/004; auditoría-1 backend C1/C2/A1/A2 + pruebas CRÍTICO monto exacto/idempotencia concurrente) · `packages/domain-hotel` (dominio puro sin I/O: `quote.ts` motor determinista noches×tarifa+IVA/ISH parametrizados con min-stay/CTA/CTD, `reservationStateMachine.ts` espejo de 0006 + rol por transición, `cancellationPolicy.ts` 4 puntos, `overbooking.ts`, `compsetGuard.ts`) + migraciones `0013` (min_stay/CTA/CTD en rate_plan, sobreventa configurable por room_type, hotel_tax_config, hotel_cancellation_policy, confirmation_code, `release_availability()`, `cancel_reservation_public()` SECURITY DEFINER) y `0014` (reservation.channel) + rutas nuevas (`POST /quotes`, `PATCH .../fechas` bajo lock, `POST .../cancelar`, `POST .../procesar-no-show` + `jobs/noShow.ts` idempotente por status='confirmada', `POST /reservas/cancelacion-publica` sin sesión de staff, CRUD `/tarifas`/`/impuestos`/`/politica-cancelacion`, `GET /disponibilidad/grid`) + frontend (`Reservas.tsx` tabla+filtros+panel lateral+crear/modificar/cancelar, `Disponibilidad.tsx` grid semanal responsive, sección de tarifas/impuestos/política en `Configuracion.tsx`) · commits `f41ab3a`, `fa49bac`, `870e51f`, `8fd252a`, `4c8c52f` (motor+API+tests iniciales) + `a092630`/`10aadc5` (A1 folio, reproducido en rojo primero), `a584b7b`/`8552439` (A2 audit_log_chain_head+FOR UPDATE, primer intento con advisory lock documentado y descartado por seguir bifurcando bajo carga real), `59f3242`/`749c4d5` (cierre explícito: cancelación con última habitación real, Idempotency-Key concurrente, monto exacto multi-noche), `d780041`/`9dd7d85` (frontend), `a3d019a` (E2E+capturas h4-*.png), `9d19709`/`e9d9558` (compuertas+docs) · pruebas: `npm run test:unit` 192/192, `test:integration` 38/38, `test:adversarial` 57/57, `npx playwright test` 20 passed/2 skipped (mismos 2 de siempre) · compuertas: lint 0 errores, typecheck 0 errores, build (api+web) OK — logs `docs/logs/h4-lint-20260906-072138.log`, `h4-typecheck-20260906-072138.log`, `h4-test-unit-20260906-072138.log`, `h4-test-integration-20260906-072138.log`, `h4-test-adversarial-20260906-072138.log`, `h4-build-20260906-072138.log`, `h4-e2e-20260906-072048.log` · 4 hallazgos de auditoría-1 cerrados con prueba de regresión (C1 cancelar/no-show libera inventario real — incluye caso "última habitación, otra reserva la toma"; C2 noche sin tarifa rechaza con `sin_tarifa` en vez de cobrar $0; A1 folio se crea al confirmar, alcanzable sin cliente admin; A2 cadena de audit_log ya no se bifurca bajo concurrencia real, tabla `audit_log_chain_head`+`SELECT FOR UPDATE` tras descartar `pg_advisory_xact_lock` por evidencia empírica) · pendiente/rojo declarado: REQ-RES-016 (check-in online, fuera de alcance de H4), REQ-RES-022/REQ-REV-008 (`scripts/checks/no-ota-directa.ts`/`orden-conectores-pms.ts` no existen — no hay conectividad OTA que auditar todavía), REQ-RES-002 completo depende de credenciales PMS reales (motor ya no usa una interfaz `PmsPort` con `MockPmsConnector`: lee `rate_plan` directo bajo RLS, documentado en README de `packages/domain-hotel`/`apps/api` como decisión explícita, no simulación), no-show sin cobro real de tarjeta (pasarela pendiente), `MEDIO` de connection pooling y captura de error real en el worker de outbox (auditoría-1/backend) no tocados en este hito · siguiente: merge de `packages/mcp-servers` (H9+H11) y reauditoría de los hallazgos aquí cerrados.
- H9+H11 (REQ-INT-001/002/003/005, REQ-BO-027/028/029, REQ-INT-007/008, REQ-RES-017, REQ-REC-009, REQ-SEG-015, ADR-007/ADR-011) · Sonnet, worktree · `packages/mcp-servers/{shared,pms,whatsapp,payments,cfdi,energy,locks}`: cada uno con puerto (interfaz+Zod+errores tipados+mapeo de estados nativo↔dominio), adaptador real "esqueleto honesto" (rutas documentadas, OAuth/Bearer, backoff exponencial+jitter, HMAC+replay de webhooks, idempotencia, rate limit) que se declara `[PENDIENTE DE CREDENCIALES]`/`[PENDIENTE DE HARDWARE]` sin inventar respuesta, y adaptador `Fake*`/`Simulated*` (`simulated:true`) con fixtures realistas (Cloudbeds, Meta WhatsApp Cloud API, Stripe/Conekta con `DualPacCfdiPort`-equivalente de failover, Finkok/SW Sapien con `DualPacCfdiPort`, Home Assistant+Shelly, Seam); `EnergyPort.setHvacState` exige `needsApproval` (guarda física 20-27°C + prioridad de 2h del huésped, en ese orden, antes de cualquier aprobación) y `LockPort` exige doble confirmación de actores distintos + evidencia del PMS (check-in pagado+identidad verificada), con `LockCommandOrigin` que excluye "voz"/regla de energía en el tipo; prueba de arquitectura (`tests/unit/mcp-servers/architecture/lock-isolation.spec.ts`) confirma 0 referencias a `mcp-locks`/`LockPort` en los otros 6 paquetes · commit pendiente en esta rama · `npm run lint` (`docs/logs/h9-lint-*.log`, 0 errores), `npm run typecheck` raíz + por paquete (`docs/logs/h9-typecheck-*.log`, 0 errores), `npm run test:unit` (`docs/logs/h9-test-unit-*.log`, 228/229, 1 `skipIf` honesto por falta de credenciales de Cloudbeds) · pendiente: las 15 pruebas de contrato quedan **pendiente de credenciales/hardware** salvo lo verificable con el fake/simulado (ningún REQ-INT se marca `hecho`); REQ-INT-004/006/009/012..015 fuera de alcance de este worktree.
- H4 · reservas/disponibilidad/tarifas (Sonnet #21) · 15 commits · compuertas verdes; cerró C1/C2/A1/A2 de backend y los 2 de pruebas · REQ-RES-004/005/007, REQ-REV-001/004; RES-002 parcial (PMS).
- MERGE H9+H11→main (Fable) · c597c35 · compuertas post-merge: lint 0 err, tsc 0, unit 303/303 (+1 skip honesto), integ 38/38, adversarial 57/57, build OK · docs/logs/post-merge-h4-h9-*.log.
- AUD-1 · corrección base de datos/seguridad multi-tenant (Sonnet) · docs/auditoria-1/correccion-bd.md · 6 hallazgos cerrados sobre `docs/auditoria-1/seguridad.md`/`datos.md` (4 CRÍTICOS: S-C1 `record_audit_log()` sin validar al actor de sesión, S-C2 `outbox`/`idempotency_key` sin scope de hotel/rol, D-C1 `room_type_id` sin FK compuesta por hotel, D-C2 `hotel_staff.org_id` sin derivar de `hotel.org_id`) + 2 ALTO/MEDIO de `backend.md` que H4 no había cerrado (worker de outbox sin causa real ni timeout por handler; conexión por request sin pool) + 3 MEDIO de `datos.md` (índice real del worker de outbox, TTL de `idempotency_key`, `hotel.timezone`) + 1 ALTO de `pruebas.md` (`npm test` no corría adversarial) · 8 migraciones nuevas expand-only `packages/db/migrations/0016`–`0023` (rango asignado 0016–0029; H5 usa 0030+, H6b usa 0040+) + `apps/api/src/outbox/worker.ts`, `apps/api/src/lib/idempotency.ts`, `packages/db/src/engines.ts` (pool de conexiones), `package.json` (`test` compuesto) · 6 commits `fix(aud-1/bd): ...` · confirmado además que el ALTO de bifurcación de hash de `audit_log` (datos.md/backend.md) ya seguía cerrado por H4 (0015, `audit_log_chain_head`+`FOR UPDATE`), reejecutando `tests/integration/audit-log-concurrencia.spec.ts` (20 escrituras concurrentes × 8 rondas, sin bifurcación) — no fue necesario reabrirlo · pruebas nuevas: `tests/adversarial/auditoria-1-bd-criticos.spec.ts` (9, los 4 CRÍTICOS con sesión RLS real), `tests/unit/api/outbox-worker.spec.ts` (+3), `tests/integration/pool-sin-fuga-de-claims.spec.ts` (2, `poolMax:1` fuerza la misma conexión física entre hoteles distintos), `tests/integration/outbox-worker-indice.spec.ts` (1, EXPLAIN sobre 8,000 filas), `tests/integration/idempotency-ttl.spec.ts` (3), `tests/unit/schema/hotel-timezone.spec.ts` (4) · compuertas: `npm run lint` 0 errores, `npm run typecheck` 0 errores, `npm test` (unit 311, integration 44, adversarial 66 = 421 pruebas verdes), `npm run build` (api+web) OK — logs `docs/logs/aud1-bd-{lint,typecheck,test,build}-20260906-075050.log` · pendiente/fuera de mandato (documentado en `correccion-bd.md`, no tocado): CORS sin restricción de origen y rate limit en memoria (seguridad.md MEDIO/BAJO, corresponden al frente backend/API), paridad visual sin comparación de píxeles (pruebas.md MEDIO, dejado explícitamente `pendiente` para el frente frontend), job de purga por lote de `idempotency_key` expirada (columna/índice ya listos, falta el scheduler).
- AUD-1 BD/seguridad · corrección (Sonnet #30) · migs 0016–0023, tests/adversarial/auditoria-1-bd-criticos.spec.ts 9/9 · 4 críticos cerrados con sesión RLS real; npm test = 421 verdes · pendientes: CORS/rate-limit y paridad visual → H8.
- H5 · folio/pagos/night audit/CFDI (Sonnet #31, worktree) · 5a1ccc9 · compuertas verdes · pendientes: PSP/PAC reales, cron night audit, bóveda identidad (REQ-REC-011) · merge tras cierre de H8 en main.
- H5 (REQ-REC-004, REQ-BO-001/002/007, REQ-REV-013; REQ-REC-008/012 y REQ-BO-006/008 parciales — ver README de apps/api para el alcance exacto) · Sonnet, worktree · Dominio: `packages/domain-hotel/src/folioEngine.ts` (cálculo determinista de cargo por concepto, IVA/ISH excluidos de propina/descuento/reverso, autorización de descuento por umbral/rol, cierre de folio saldo-cero/cuenta-por-cobrar) + `fiscalHospedaje.ts` (7 funciones puras parametrizadas: IVA, ISH QRoo, DSA por cuarto-noche, ISN, ISR provisional, DIOT, retención de plataformas digitales 2026). Migraciones `0030_folio_engine.sql` (concepto/reverso/transferencia trazables en `charge`, `mark_charge_reversed()` SECURITY DEFINER, folio split vía `is_primary`/índice único parcial, `payment.status`/`token_ref` con CHECK anti-PAN, `hotel_tax_config.discount_threshold`/`dsa_per_night`/`rfc_emisor`), `0031_night_audit.sql` (`night_audit_run` + `night_audit_claim`/`night_audit_finish` con advisory lock, solo escribibles por esas 2 funciones), `0032_cfdi_emision.sql` (registro de emisiones sobre el `CfdiPort` de H9/H11, únicos parciales por folio/pago para idempotencia real), `0033_calendario_fiscal.sql` (`fiscal_obligation`+`sat_filing_approval`, trigger que bloquea `presentada` sin aprobación de owner/gm cuando `requiere_efirma`). API: `routes/folios.ts` reescrito (cargos por concepto, descuentos con autorización verificada contra `hotel_staff`, reverso/transferencia/split que nunca borran, pagos vía `PaymentProviderPort` con token opaco obligatorio en tarjeta, cierre de folio), `routes/night-audit.ts` + `jobs/nightAudit.ts` nuevos (idempotente por `business_date`, reutiliza `jobs/noShow.ts` que ahora también postea la penalización de no-show al folio), `routes/cfdi.ts` nuevo (timbrado/complemento de pago/cancelación vía `CfdiPort`, reglas de extranjero/global/no-show/anticipo de REQ-BO-001). `apps/api/src/app.ts` instancia `FakeStripeAdapter`/`DualPacCfdiPort` únicos por proceso cuando no hay credenciales reales (`ResolvedAppDeps`). Frontend: `/recepcion` gana la sección "Folio del huésped en casa" (`components/folio/FolioPanel.tsx`: cargos/pagos/descuentos/reverso/cierre con diálogos de confirmación) y `/back-office` gana "Cierre diario" + "CFDI emitidos" (estado real, nunca inventado). Hallazgo corregido de paso: `node --experimental-strip-types` no soporta *parameter properties* de TS (usadas en `packages/mcp-servers/payments|cfdi`) — el servidor real nunca arrancaba en cuanto `app.ts` importó esos paquetes; cambiado a `--experimental-transform-types` en `apps/api/package.json` y en los 3 spawns de `tests/e2e/*.spec.ts` que levantan el servidor real (h4/h5/login). Pruebas nuevas: 24 archivos (2 unit, 13 integración, 3 adversariales, 1 E2E, 2 scripts de revisión estática `no-delete-events.ts`/`no-pan-storage.ts`); night audit corrido 2 veces = mismo resultado sin duplicar cargos (secuencial y concurrente), pago con la misma Idempotency-Key concurrente = 1 sola escritura (efectivo y tarjeta), timbrado del mismo folio 2 veces = mismo UUID (secuencial y concurrente), reverso/transferencia/split verificados por conteo de filas (nunca decrece) · compuertas: `npm run lint` 0 errores, `npm run typecheck` 0 errores, `npm run test:unit` 327/328 (+1 skip heredado), `npm run test:integration` 71/71, `npm run test:adversarial` 68/68, `npm run build` OK, Playwright 22 passed/2 skipped (heredados) con capturas `h5-recepcion-folio-*`/`h5-recepcion-folio-pagado-*`/`h5-back-office-*` en 1280×800 y 390×844 — logs en `docs/logs/h5-*-20260906-082126.log` · pendiente/declarado explícitamente (ver apps/api/README.md §H5): adaptador real de PSP/PAC (créditos/CSD pendientes), endpoint que genere el link de pago tokenizado del PSP real, cron real de night audit (se dispara por endpoint), CRUD de API para `fiscal_obligation`/`sat_filing_approval` (verificado solo contra BD por ahora), `CfdiRelacionados` tipo 07 real hacia el PAC (el puerto actual no expone ese campo, se registra en nuestra tabla), REQ-REC-011 (bóveda de identidad) fuera de alcance de este hito.
- H8 (REQ-OBS-001..005 parcial, REQ-QA-007/008/009 parcial, ADR-008/009; cierra auditoria-1/seguridad.md [MEDIO] CORS + [BAJO] rate limit, y auditoria-1/pruebas.md [MEDIO] paridad visual sin comparación) · Sonnet, en `main` (índices compartidos `apps/api/src/app.ts`, `package.json`, ediciones mínimas) · **Observabilidad**: `apps/api/src/metrics.ts` (`/metrics` formato Prometheus hecho a mano, sin dependencia nueva: latencia histograma por ruta/método/status, `http_errors_total`, `outbox_pending`/`outbox_dead_letter` en vivo, `approvals_pending` que detecta si `public.approval` existe aún, `reservations_created_total`), `apps/api/src/lib/moneyAlert.ts` (`nivel:"alerta"` en logs para 5xx en rutas de pagos/cargos/cfdi/folios/reservas, cableado UNA vez en el middleware de logging de `app.ts` para no duplicar), `logger.ts` ampliado (`REDACT_PATHS` cubre email/teléfono/RFC/CURP/documento además de password/token, `createLogger` inyectable para pruebas), `/health`+`/ready` ya existían (H2) y se dejan igual (verificados de nuevo). **CORS/seguridad**: `env.ts` exige `CORS_ALLOWED_ORIGINS` explícito (nunca `*`, obligatorio en producción); `app.ts` usa `hono/cors` con esa lista + `hono/secure-headers` (HSTS solo prod, `X-Content-Type-Options`, CSP `frame-ancestors 'none'`); rate limit ahora manda `Retry-After` (segundos reales de `resetAt`) en el 429 (`errors.ts` con cabeceras adjuntas al `ApiError`). **Paridad visual real**: `tests/e2e/paridad-visual.spec.ts` reescrito — compara variables CSS de tokens, tipografías computadas, hash de geometría del logo (SVG) y `border-radius` del botón contra `tests/e2e/fixtures/restaurantes-referencia.json` (fixture con procedencia documentada: doc + código fuente de `atiende-restaurantes` leído en solo lectura), más un test adicional que intenta el render vivo de `atiende-restaurantes` (best-effort, mismo patrón que `paridad-restaurantes-login.spec.ts`) y compara igual. **Runbooks**: `docs/runbooks/{operacion,incidentes,backups-restauracion,migraciones,despliegue}.md`. **Backups**: `scripts/backup.sh`/`restore.sh` (wrappers de `scripts/backup.ts`/`restore.ts` en Node, usan `@atiende-hoteles/db`); `pg_dump`/`psql`/`pg_restore` NO vienen con el paquete `embedded-postgres` (solo `postgres`/`pg_ctl`/`initdb`) — se resolvió instalando `libpq` vía Homebrew (gratis, local); prueba real ejecutada: dump de una BD migrada+sembrada (23 tablas, 336 filas) → restore en BD nueva → conteo idéntico en las 23 tablas (`docs/logs/h8-backup-restore-real.log`). **Migraciones expand-only**: `scripts/check-migraciones.ts` (falla si una migración del manifiesto `scripts/checks/migraciones-checksums.json` cambió de hash, o si una migración nueva trae `DROP COLUMN`/`DROP TABLE`/`TRUNCATE`/`ALTER COLUMN TYPE` sin marcador `CONTRACT-APPROVED`) + `npm run check:migraciones`; no se usó ninguna migración nueva (H8 no tocó esquema). **`.env.example`**: `apps/api/.env.example` y `apps/web/.env.example`, más política de rotación de secretos documentada en `docs/runbooks/operacion.md` §6. **CI**: `.github/workflows/ci.yml` (lint→typecheck→test[unit+integración+adversarial con embedded-postgres real]→build→e2e[Playwright, instala canal `chrome` real]→npm-audit[`--audit-level=high`], con `check:migraciones` dentro del job de test y artifacts de capturas/trazas); YAML validado con `npx yaml-lint` (`action-validator` no resolvió vía npx, documentado). **npm audit**: 1 alta preexistente (`postcss` en `apps/web`, CVEs de XSS/path-traversal sin alcance real porque solo corre en build sobre CSS propio) resuelta subiendo la versión fijada a `8.5.28` (la misma ya deduplicada en el resto del árbol) — `npm audit --audit-level=high` pasó de 1 alta a **0 vulnerabilidades a cualquier severidad**, documentado en `docs/auditoria-1/dependencias.md`. Efecto lateral necesario: `login-real-y-resumen.spec.ts`/`h4-reserva-real-desde-ui.spec.ts` (H2/H4) spawnan `apps/api` en un puerto dinámico — con el CORS nuevo había que pasarles `CORS_ALLOWED_ORIGINS` explícito (fix mínimo, 2 archivos de `tests/e2e`, sin tocar rutas de negocio). Pruebas nuevas: `tests/unit/api/logger-redact.spec.ts` (4), `tests/integration/api/observabilidad.spec.ts` (15: CORS, cabeceras, `/metrics`, `/health`/`/ready`, alerta camino del dinero), `tests/unit/check-migraciones.spec.ts` (9), + 1 aserción de `Retry-After` añadida a `tests/adversarial/reserva-concurrencia-y-limites.spec.ts` · compuertas: `npm run lint` 0 errores/1 warning preexistente, `npm run typecheck` 0 errores, `npm test` (unit 323 (+1 skip preexistente), integration 59, adversarial 66 = 448 verdes), `npm run build` OK, `npm run test:e2e` 25 passed/3 skipped honestos (paridad viva sin repo hermano en un runner sin ese path, y los 2 `test.skip` ya existentes) — logs `docs/logs/h8-ci-{lint,typecheck,test,build,e2e,npm-audit,check-migraciones}.log`, `docs/logs/h8-backup-restore-real.log`, `docs/logs/h8-ci-yaml-lint.log` · pendiente/fuera de mandato explícito de este hito (no simulado): REQ-OBS-001/002 (SLOs de canal WhatsApp/PMS/voz — no hay esos conectores con tráfico real todavía, solo se construyó el `/metrics` genérico); REQ-OBS-003/004/005 (bucle de auditoría periódica de 12 rubros, H10); `approvals_pending` en 0 hasta que exista `public.approval` (H6b); job/cron real que llame `drainOutboxOnce` en bucle (documentado en runbook de operación como pendiente, hoy manual); escaneo de secretos dedicado en CI (`gitleaks`, REQ-SEG-012 — GitHub Secret Scanning nativo queda como cobertura parcial una vez el repo se publique) · siguiente: merge de H5/H6b sobre este `main` (sus migraciones 0030+/0040+ no chocan con lo hecho aquí); cuando H6b aterrice `public.approval`, `approvals_pending` empieza a reportar de verdad sin tocar `metrics.ts`.
- MERGE H5→main (Sonnet #34) · 1cbbc6d · compuertas: lint 0 err, tsc 0, unit 347/348, integ 92, adversarial 77, build OK, e2e 27/3skip · 27 migraciones aplican desde cero.
- H6b (REQ-HK-001/002/003/011/013/014/020 parciales, REQ-AGT-001/002/003, REQ-HUE-001/002 sin credenciales, ADR-006/007) · Sonnet, worktree · **BD** (`packages/db/migrations/0040-0045`): `room.housekeeping_status`+`room_housekeeping_status_event` (historial, distinto de `room.status`/H1), `housekeeping_task` (RLS: housekeeping solo sus tareas asignadas), `agent_approval`+`agent_approval_confirmation` (respaldo persistente de `ApprovalQueue`, decidir reservado a owner/gm), `maintenance_ticket` (origen huésped/staff/agente/sensor, RLS: housekeeping reporta pero solo ve sus propios reportes — necesario porque `INSERT...RETURNING` exige también la política de SELECT, hallazgo real reproducido con `pg_policy`/`withAppSession` antes de corregir), `hotel_messaging_config`+`conversation`+`message` (WhatsApp simulado, `message.simulated=true`) · **agent-core**: `PostgresApprovalQueue` (misma suite de contrato que `InMemoryApprovalQueue`, `tests/support/approvalQueueContract.ts`, corrida contra ambas; sobrevive un reinicio REAL del proceso embedded-postgres) + 4 tools de dominio (`crear_tarea_housekeeping`, `crear_ticket_mantenimiento` con dedupe 24h, `autorizar_gasto_mantenimiento` money/needsApproval, `enviar_mensaje_whatsapp_plantilla` external/needsApproval con `createTransactionalTemplateApprovalQueue` para auto-aprobar plantillas transaccionales por configuración de hotel sin violar GOB-026) + journey `AgentRunner`+`FakeProvider` de punta a punta en shadow/propone/autopilot (`tests/integration/agent-core/journey-checkin-incidencia.spec.ts`) · **defecto real encontrado y corregido**: 3 archivos de agent-core (H6a) y 4 de `packages/mcp-servers/{shared,whatsapp}` (H9) usaban el azúcar "parameter properties" de TypeScript, incompatible con `node --experimental-strip-types` (runtime real de `apps/api`) — `apps/api` literalmente no arrancaba en cuanto se conectó a agent-core+WhatsApp; corregido a campos explícitos en los 7 archivos, verificado arrancando el proceso real (no solo vitest, que usa esbuild y no lo detecta) · **API** (`apps/api/src/routes/{housekeeping,mantenimiento,aprobaciones,mensajeria}.ts`): tablero/ciclo de tarea, tickets con "cerrar con costo" que SIEMPRE abre una solicitud de aprobación (nunca cierra directo), bandeja de aprobaciones que ejecuta la tool correspondiente al completarse la(s) confirmación(es) (input real recuperado de `agent_approval.input_json`), mensajería con plantilla transaccional sin espera humana y webhook público (HMAC + idempotencia real por `event_id` vía `idempotency_key`, replay→200 sin reprocesar) · **frontend**: `/housekeeping` (tablero móvil real, tarjetas en vez de tabla en 390px), `/mantenimiento`, `/mensajeria` (hilo + badge "simulado" + configuración de plantillas transaccionales), `/aprobaciones` (nueva página + badge de pendientes reales en el encabezado) · commit `70fc720` · pruebas nuevas: `tests/integration/agent-core/{postgres-approval-queue,journey-checkin-incidencia}.spec.ts` (19), `tests/integration/api/{housekeeping-mantenimiento,mensajeria}.spec.ts` (13, incluye regresión del cast de enum en `GET /aprobaciones?estado=`), `tests/adversarial/housekeeping-mantenimiento-mensajeria.spec.ts` (11: aislamiento cross-hotel, camarista no ve/cambia costo ajeno, decidir reservado a owner/gm, webhook firma inválida/sin firma/replay), `tests/e2e/h6-housekeeping-mantenimiento-mensajeria.spec.ts` (recorrido real completo, capturas `h6-{housekeeping,mantenimiento,mensajeria,aprobaciones}-{1280x800,390x844}.png`) + 4 rutas nuevas agregadas a `axe-accesibilidad.spec.ts` · compuertas: lint 0 errores, typecheck 0 errores (raíz+e2e+api), `test:unit` 303/303 (+1 skip), `test:integration` 70/70, `test:adversarial` 68/68, build (api+web) OK, Playwright 20/20 (desktop+mobile, h6 + axe) — logs en `docs/logs/h6b-*-20260906-083619.log` · pendiente/rojo declarado: WhatsApp SIEMPRE `FakeWhatsappAdapter` (PENDIENTE DE CREDENCIALES de Meta, ver `apps/api/README.md`), sin agente conversacional LLM real conectado a estas tools todavía (`EnvProvider` sigue honesto/no implementado, H9), sin columna de "piso" en `room` (tablero ordena por código), REQ-HK-001 (asignación diaria CP-SAT)/003 (visión)/005-010/012/015-019/021-022 fuera de alcance de este hito (no se fabricó ninguna cifra de esas capacidades) · siguiente: reauditoría de agent-core+mcp-servers si corresponde por el fix de sintaxis; conectar `EnvProvider`/adaptador Meta real cuando existan credenciales.
- MERGE H6b→main (Sonnet #34) · 1a1c3b9 · 33 migraciones desde cero; unit 347, integ 124, adversarial 88, e2e 37/3skip · main contiene H1–H6, H8, H9, H11 · siguiente: H7 + cierre P0 pendientes → auditoría-2 (12 rubros).
- H7 (REQ-AGT-003 hecho, REQ-AGT-020 hecho, REQ-AGT-022 verificado por adversarial, REQ-REV-018 parcial/captura sin línea base firmada; REQ-AGT-004/006 reutilizan lo ya construido en agent-core H6a/aud-1; REQ-AGT-009/REQ-BO-006/REQ-GOB-014 fuera de alcance de este hito, quedan `pendiente`) · Sonnet, en `main` (rutas en archivos nuevos, edición mínima de `app.ts`/sidebar) · **BD** (`packages/db/migrations/0024-0026`, rango asignado 0024–0029): `agent_run` (una fila por corrida agregada — tokens/costo/steps/duración/estado terminal/`request_id`, append-only, `agent_cost_mes(hotel, agente)` que suma el mes en curso), `agent_config` (gate shadow/propone/autopilot + techo mensual USD + `alert_threshold_pct` por (hotel, agente), RLS de escritura reservada a owner/gm), `roi_event` (REQ-AGT-003/H17-001: `monto_estimado`/`monto_verificado`/`metodo_contrafactual`/`confianza`/`supuesto_version`, columna `estimado` recalculada por trigger a partir de si hay verificado — nunca confiada a la app) · **agent-core**: `agents.ts` (catálogo de 3 agentes como configuración — `recepcion_virtual` canal/Sonnet 5, `enrutador_mensajes` Haiku 4.5 sin tools, `auditor_nocturno` batch/Opus 5 — cada uno con `toolNames`/`allowedStaffRoles`/gate y techo por defecto, techos dentro de la banda LLM-026 USD 27-158/mes) + tool `registrar_evento_roi` (effect="write", sin aprobación, rechaza si no trae monto estimado NI verificado) · **API** (`apps/api/src/routes/{agentes,roi}.ts`, 2 archivos nuevos + 4 líneas en `app.ts`): `POST /hoteles/:hotelId/agentes/:agente/ejecutar` construye el `ToolContext` SOLO de la sesión (body `.strict()` rechaza `hotelId`/`gate`/`orgId`), corta por presupuesto ANTES de invocar al proveedor (`agent_cost_mes` vs techo, respuesta honesta `presupuesto_agotado` con fila de `agent_run` costo 0), usa `EnvProvider` real (sin credenciales → `no_configurado` honesto) salvo `demo:true` que corre un guion determinista de `FakeProvider` (check-in con incidencia) etiquetado `simulado:true`; cada `AgentTraceEvent` (ya redactado por `redact()` dentro de agent-core) se escribe en `audit_log` vía `record_audit_log()` y el resumen agregado en `agent_run`, ambos en la MISMA transacción por-request; `GET /agentes` (catálogo+gate resuelto), `GET /agentes/costos` (consumido/techo/`pctUsado`/alerta 80%/"sin datos" honesto por CONTEO de corridas, no por costo=0), `PATCH /agentes/:agente/config` (owner/gm), `GET /roi` (eventos+suma+`supuestoVersion`) · métrica Prometheus `agent_cost_usd_total{hotel,agente}` (`apps/api/src/metrics.ts`) · **frontend**: página nueva `/agentes` (gate/techo editable por owner/gm, barra de costo del mes o "sin corridas todavía", botón "Demo (simulada)" con resultado en vivo) + 1 línea en el sidebar (`AppShell.tsx`) + 1 ruta en `App.tsx`; tarjeta "Valor generado por agentes de IA" en `/resumen` con badge "estimado, supuestos H17-v1" y enlace a `/agentes`, "sin datos todavía" honesto cuando no hay `roi_event` · pruebas nuevas: `tests/unit/agent-core/{agents,roi-tools}.spec.ts` (11: selección de modelo por rol/agente, catálogo cerrado, tool sin DB si falta monto), `tests/integration/api/agentes.spec.ts` (7: shadow no ejecuta escritura + `agent_run`+`audit_log` en la misma transacción, `.strict()` rechaza campos del cliente, presupuesto agotado corta ANTES del proveedor con `agent_run` costo 0/sin pasos, `no_configurado` sin demo, costos/ROI reales), `tests/adversarial/agentes-aislamiento.spec.ts` (9: hotel A no ejecuta/lee sobre hotel B ni con `X-Hotel-Id` falsificado, housekeeping no ejecuta `auditor_nocturno` ni cambia config de ningún agente, frontdesk sí puede recepción_virtual confirmando que el 403 es por rol, body con `hotelId`/`orgId`/`gate` desconocidos → 400, gate efectivo siempre sale de `agent_config`/default nunca de una corrida previa), `tests/e2e/h7-agentes.spec.ts` (recorrido real completo con capturas `h7-{agentes-catalogo,agentes-demo,resumen-roi}-{1280x800,390x844}.png`) + 1 ruta nueva en `axe-accesibilidad.spec.ts` · compuertas: `npm run lint` 0 errores/1 warning preexistente ajeno, `npm run typecheck` 0 errores, `npm test` (unit 357, integration 131, adversarial 97 = 585 verdes), Playwright 22/22 (h7 + axe, desktop+mobile) — logs en `docs/logs/h7-{typecheck,lint,test,e2e}.log` · pendiente/rojo declarado (no simulado): REQ-AGT-009 (prompt injection/red-teaming en CI, requiere su propio simulador de huéspedes, fuera de alcance de este hito), REQ-BO-006 (aprobación de e.firma para presentación SAT, dominio fiscal distinto al runtime de agentes, sin tocar en H7), REQ-REV-018 completo (línea base firmada + activación de cobro por resultado sobre `roi_event` — la CAPTURA con supuesto versionado sí existe, la lógica de facturación no), `EnvProvider` sigue sin integración real de proveedor (mismo estado honesto desde H6a: con credenciales declara "pendiente de implementación", sin ellas "no configurado"), sin corrida nocturna programada real del `auditor_nocturno` (se dispara manualmente vía API/demo, sin scheduler) · siguiente: cierre de P0 pendientes declarados en hitos previos → auditoría-2 (12 rubros).
- H7 · runtime de agentes en API (Sonnet #35) · 9a5fa63 · 585 tests backend verdes, 22 Playwright · pendientes declarados: red-teaming (AGT-009), e.firma (BO-006), scheduler real del auditor nocturno.
- Trazabilidad (Sonnet) · derivé el Estado canónico de los 276 REQ de `docs/REQUISITOS.md` a partir de `docs/ACEPTACION.md` (∈ {hecho, parcial, pendiente-credenciales, pendiente-hardware, pendiente-decisión, pendiente}) y copié la evidencia (ruta de prueba + log) a su columna Evidencia · `docs/TRAZABILIDAD.md` (nuevo) · verifiqué el 100% de la población de renglones "hecho" de ACEPTACION.md (23, no ≥40 como asumía la tarea — población real menor): 23/23 con evidencia existente y log verde, pero 5 degradados a `pendiente-credenciales`/`parcial` por criterio estricto de ADR-007 (BO-001, BO-002, BO-006, UX-006, REV-013); paridad de IDs REQUISITOS↔ACEPTACION diff vacío (276=276) y conteos de cabecera de REQUISITOS.md siguen cuadrando · resultado: 18 hecho (6.5%), 19 parcial, 87 pendiente-credenciales, 20 pendiente-hardware, 2 pendiente-decisión, 130 pendiente; P0 128 con solo 15 hecho = **11.7%** · hallazgo adicional: ACEPTACION.md nunca fue actualizado tras H7 para REQ-AGT-003/020/022 (evidencia verde real en `docs/logs/h7-test.log` no reflejada) — documentado, no reclasificado unilateralmente · siguiente: actualizar ACEPTACION.md para REQ-AGT-003/020/022; profundizar clasificación `pendiente-decisión` sobre el catálogo completo de REQ-GOB-012.
- Cierre de P0 sin dependencia externa (Sonnet, worktree, en paralelo a H7 en `main` — nunca se tocó `agentes.ts`/`roi.ts`/migraciones 0024-0029) · `docs/cierre-p0/inventario.md` (Paso 1: inventario completo ordenado por riesgo de todo REQ P0/`ninguna` pendiente, con justificación caso por caso de los que en realidad requieren credencial pese a la columna) · migraciones `0050`-`0054`, 11 REQ/frentes cerrados con 1 commit cada uno: **REQ-TEN-004** (`0050`, catálogo de experiencias + `order_experience_public()` SECURITY DEFINER sin ningún parámetro de precio — el body con `precio=0.01` es literalmente ignorado, `tests/adversarial/rpc-security-definer.spec.ts`); **REQ-RES-022**/**REQ-REV-008** (`scripts/checks/no-ota-directa.ts` + registro único `PMS_CONNECTOR_REGISTRY` con orden Cloudbeds→Mews→SiteMinder→OHIP, `scripts/checks/orden-conectores-pms.ts`); **REQ-GOB-009** (`scripts/checks/no-comandos-destructivos-agente.ts`, 0 `supabase db push`/`git push --force` fuera de `.github/workflows`); **REQ-GOB-007** (`packages/agent-core/src/backlog/backlogStateMachine.ts`, máquina de estados pura con transición inválida rechazada, 3 intentos→`blocked` automático, "una tarea = un archivo" con round-trip); purga por lote de `idempotency_key` (auditoria-1/datos, dejado pendiente explícitamente: `apps/api/src/jobs/purgeIdempotencyKeys.ts` + CLI); **REQ-REV-013 cron** (`apps/api/src/jobs/nightAuditScheduler.ts`, lock en proceso por hotel + `night_audit_claim` transaccional entre procesos, arrancado en `server.ts` y por CLI `scripts/run-night-audit-scheduler.ts`); **REQ-REC-011**/**REQ-SEG-014**/**REQ-SEG-004** (`0051`, bóveda de identidad: AES-256-GCM en código de aplicación sin pgcrypto, `identity_vault` sin GRANT a `authenticated` — consulta directa da "permission denied" —, `identity_ref` con solo nombre/nacionalidad/tipo/últimos4, MRZ TD3 validada por dígitos de control real en `packages/domain-hotel/src/mrz.ts`, purga ≤30 días post-checkout; doble control pleno de SEG-014 declarado explícitamente NO implementado); **REQ-UX-005** (`0052`, conocimiento local editable por gerencia, reflejo <30s medido); **REQ-UX-004** (backend real para la UI de `BackOffice.tsx` que ya existía sin ruta — `roi_event` de H7 verificado en runtime vía `to_regclass`, nunca simulado); **REQ-UX-006** (`0053`, botones interactivos de WhatsApp — `MessagingPort` extendido + `staff_user.whatsapp_phone` + webhook que reutiliza la MISMA lógica de decisión del panel web, offline contra `FakeWhatsappAdapter`); **REQ-RES-016** (`0054`, check-in online de un solo uso reutilizando `register_identity_document()` sin duplicar, MRZ real, rechazo de chat libre con redirección). Commits `5173447`, `e14a4c9`, `8092d19`, `cf34d71`, `7b9873e`, `5920d68`, `9e2f0f6`, `47c2688`, `85e1d8e`, `ef9c419`, `9e3ca9c`, `db0c954`, `c0b2bcb` · pruebas nuevas: 8 archivos de test en `packages/db/migrations` nuevos + ~90 pruebas nuevas (adversarial: rpc-security-definer 5, boveda-identidad 8, checkin-chat-libre 3; integración: idempotency-purga-por-lote 3, night-audit-scheduler 8, conocimiento-local/latencia 5, back-office-cobros 4, contracts/whatsapp/aprobaciones-boton 8, checkin-online 5; unit: gob/backlog-state-machine 21, gob/no-comandos-destructivos-agente 7, mcp-servers/pms/registro-conectores 4, domain-hotel/mrz 13, domain-hotel/checkin-free-text-guard 6, api/identity-encryption 8) · compuertas: `npm run lint` 0 errores, `npm run typecheck` 0 errores, `npm test` 48+34+17 archivos = 667 pruebas verdes (+1 skip heredado), `npm run build` (api+web) OK — logs `docs/logs/p0-{lint,typecheck,test,build}-20260906-101549.log` · `test:e2e` NO se corrió (ningún `.tsx` fue tocado en este frente) · pendiente/declarado (ver inventario.md §1/§2, ~70 REQ P0 restantes documentados con razón real, no una lista arbitraria): REQ-UX-001/002/003 (rediseño/auditoría integral de `apps/web`, proyecto propio), REQ-BO-010/014/024/025/035 (dependen de módulos de negocio — P&L real, GM Copilot, RR.HH. — que no existen todavía), REQ-HUE-005/006/009/014/016/017/021/023 y REQ-RES-001 (requieren el agente conversacional de WhatsApp/voz real, credencial Meta/Telnyx pese a que REQUISITOS.md los marque "ninguna"), REQ-GOB-001..006/012/013 (reglas de proceso sin superficie de código propia todavía), REQ-OBS-001/002/003/004/005/007 (dependen de canales reales o del motor de ROI de H7) · siguiente: auditoría-2 (12 rubros) cuando H7 cierre; reconciliar migraciones 0024-0029 (H7) con 0050-0054 (este frente) al fusionar a `main`.
- MERGE P0→main (Fable) · 5eaac82 · compuertas: lint 0 err, tsc 0, db:reset OK (38 migraciones), unit 416/417, integ 164, adversarial 113, build OK, e2e 41/3skip · main contiene H1–H9, H11 y cierre P0.
- AUD-2 (Fable) · ronda COMPLETA de 12 rubros sobre snapshot 488a22a + reconciliación de trazabilidad en paralelo · siguiente: verificación de hallazgos, corrección críticos/altos, reauditoría, síntesis y tablero.
- TRAZABILIDAD (Sonnet #37) · docs/TRAZABILIDAD.md · cobertura REAL: 18/276 hecho, P0 15/128 (11.7%); 87 REQ bloqueados por credenciales y 20 por hardware (no ejecutables aquí); 43 P0 pendientes sin razón externa = trabajo ejecutable restante tras la corrección de auditoría-2. Fable: esta cifra es la que manda, no la cuenta de hitos.
- AUD-2 (Fable) · 12/12 informes: frontend 6, backend 4, agéntico 3, tool-calling 3, seguridad 2, fiscal 2, legal 3, arquitectura 6, pruebas 7, operabilidad 5, rendimiento 4, datos 3 → media 4.0 · CRÍTICOS: 20 (seguridad 4, fiscal 3, legal 3, datos 3, backend 2, agéntico 2, tool-calling 2, rendimiento 1) · corrección en curso: lote A (seguridad/legal/datos) en main, lote B (fiscal/agéntico/tool-calling/backend/rendimiento) en worktree; lote C pendiente (frontend, operabilidad, arquitectura, pruebas ALTOS).
- AUD-2 lote A (Sonnet, seguridad+legal+datos) · verificado con sesión RLS real y corregido en `packages/db` (migraciones `0060`-`0069` expand-only): S1/D1 CRÍTICO (checkin_link secuestra reserva de otro hotel, reproducido por API) cerrado con FK compuesta `(hotel_id, reservation_id)`→`reservation(hotel_id,id)` + `complete_checkin_public()` usa el hotel de la reserva, nunca del enlace; D2 ALTO (`reservation.guest_id` sin scope) cerrado con FK compuesta a `guest(hotel_id,id)`; S2 CRÍTICO (`mark_charge_reversed` sin validar hotel) y S3 CRÍTICO (`night_audit_claim/finish` sin validar hotel + re-terminaba corridas completadas) y ALTO (`set_identity_checkout` sin validar hotel) cerrados validando membresía real del actor contra el recurso; S4 CRÍTICO (`sat_filing_approval` forjable con doble membresía) cerrado con FK compuesta a `fiscal_obligation(hotel_id,id)`; D3 CRÍTICO (folio cerrado admite cargo por carrera, 10/10 reproducido) cerrado con dos triggers que comparten el lock de fila del folio (`charge_reject_on_closed_folio`/`folio_reject_inconsistent_close`) — nunca se tocó `apps/api/src/routes/folios.ts` (lote B). Legal: L1 CRÍTICO (purga de bóveda de identidad nunca corría) cerrado con `jobs/purgeIdentityVaultScheduler.ts` cableado en `server.ts` (lock por hotel + métrica `identity_vault_purged_total` + `audit_log`); L2 CRÍTICO (aviso de privacidad no declaraba transferencia a IA) cerrado en `apps/web/src/pages/Privacidad.tsx`; L3 ALTOS: tabla `consent`+`record_consent()`, check-in online exige `consentimientoAvisoPrivacidad` y lo registra, ARCO mínimo (`apps/api/src/routes/privacidad.ts`: export por enlace de un solo uso + ticket auditado con SLA de 20 días), retención configurable de `conversation`/`message` por hotel (`hotel_messaging_config.conversation_retention_days` + `jobs/purgeConversationsScheduler.ts`, default 730 días `pendiente-decision`). Consentimiento en primer contacto WhatsApp y opt-out de marketing en `messagingTools.ts` quedan `pendiente-coordinación` con lote B (mensajeria.ts/agent-core fuera de mi alcance). Nueva página pública `apps/web/src/pages/CheckinPublico.tsx` (no existía ninguna). Prueba: `tests/adversarial/auditoria-2-lote-a-seguridad-legal.spec.ts` (17/17, incluye verificación roja→verde de D3 desactivando 0066 temporalmente) + toda la suite existente sigue en verde · commits ver `docs/auditoria-2/correccion-A-seguridad-legal.md` · siguiente: lote B/C completan fiscal/agéntico/tool-calling/backend/rendimiento/frontend/operabilidad/arquitectura/pruebas; coordinar consentimiento WhatsApp + opt-out de marketing con quien toque `mensajeria.ts`/`messagingTools.ts`.
- USUARIO 2026-09-06: alcance ampliado a 'listo para promoción' (Google auth, correos con plantilla, todo punta a punta). Registrado D-005; despacho H12a y H12b en worktrees mientras terminan los lotes A/B/C.
- USUARIO 2026-09-06: 'todo, checa cómo están armados Likida y Atiende Restaurantes' → inventario exhaustivo (#55) antes de H12b; H12a (#53) ya en curso con las plantillas de correo de Restaurantes y el flujo de Google como espejo.
- AUD-2 lote A (Sonnet #50) · 15 commits · 4 CRÍT seguridad + 3 CRÍT datos + 2 CRÍT legal cerrados; consent/ARCO/retención implementados; pendiente-coordinación: opt-out marketing y enlace en disclosure (lote B) · e2e 42/44 con 2 flakes de contención → seguimiento en reauditoría.
- INVENTARIO (Sonnet #55) · docs/referencia/08-inventario-punta-a-punta.md · 29 LAUNCH · despacho H12b (#56) y H12c (#57) para cubrir los huecos ejecutables; los que exigen credenciales quedan cableados con dobles.
- H12c (REQ-LAUNCH-001..015, LAUNCH-015/017/023/025/026 de `08-inventario-punta-a-punta.md`) · Sonnet, worktree, en paralelo a H12a (auth/registro/correo/onboarding) y H12b (admin/deploy/supabase/CSP/PWA) — no se tocaron sus archivos. **Facturación SaaS**: `packages/db/migrations/0110-0114` (`plan` con 3 filas Starter/Pro/Enterprise `es_propuesta=true`, `subscription` por org con trial de 14 días, `check_entitlement()`/`entitlement_usage()` fail-closed si no hay suscripción, `invoice_saas`, `billing_webhook_event` con idempotencia persistida, triggers de notificación) + `packages/mcp-servers/billing` (`BillingProviderPort` con `StripeBillingAdapter`/`ConektaBillingAdapter` esqueleto honesto + `FakeBillingAdapter`) + `apps/api/src/routes/suscripcion.ts` (estado/checkout/portal/facturas/webhook HMAC+idempotente) + `apps/api/src/lib/entitlement.ts` (middleware `requireEntitlement` listo para cablear en las rutas de creación de hotel/habitación/agente/mensaje, propiedad de otros agentes en paralelo — documentado como pendiente de esa integración, no simulado) · **Notificaciones in-app**: `packages/db/migrations/0113-0114` (tabla `notification`+`notification_preference`, RLS por destinatario/rol/hotel, triggers de BD para reserva confirmada/aprobación pendiente/ticket urgente/night audit cerrado/límite de plan al 80%, `mark_all_notifications_read()` atómico) + `apps/api/src/routes/notificaciones.ts` + `apps/web/src/components/NotificacionesBell.tsx` (campana con conteo real) + página `/notificaciones` · **Analítica/errores por puerto**: `packages/analytics` (`AnalyticsPort`/`ErrorReporterPort`, PostHog/Sentry esqueleto honesto + fake, catálogo cerrado de eventos, bloqueo de PII por clave vedada y por patrón de contenido) + `apps/web/src/lib/analytics.ts` + `CookieConsentBanner.tsx` (opt-in explícito, activa el adaptador real SOLO tras consentimiento) · **Landing pública**: `apps/web/src/pages/Landing.tsx` en `/` (antes redirigía a `/resumen`), hero con identidad de marca, 5 secciones con capturas reales del producto (`tests/e2e/screenshots` copiadas a `apps/web/public/landing/`), planes (propuesta) consumidos de `GET /planes` (público a propósito), FAQ, CTA a `/registro`/`/login`, footer legal; `index.html` con `robots: index,follow` + OG/Twitter completos (antes `noindex,nofollow` en todo el sitio) · commit `wip` guardado por el orquestador tras un corte 429 del servidor durante la sesión (63 archivos, ver `git show --stat`) · pruebas nuevas: `tests/adversarial/facturacion-saas.spec.ts` (6: trial sembrado, org ajeno no ve la suscripción de otro ni por HTTP ni por RLS directa, webhook firma inválida→403, webhook repetido→1 sola factura, límite de plan excedido→402 con mensaje, org sin suscripción→402 fail-closed), `tests/adversarial/notificaciones.spec.ts` (4: emisión real desde trigger, rol sin destino no ve la notificación, hotel A no ve las de hotel B, marcar-todo-leído atómico), `tests/adversarial/analytics-sin-pii.spec.ts` (6: bloqueo por clave vedada y por patrón de contenido, catálogo cerrado de eventos), `tests/e2e/h12c-landing.spec.ts` (render sin scroll horizontal, 6 imágenes de producto cargadas, CTAs correctos, banner de cookies) + ruta `landing`/`suscripcion`/`notificaciones` añadidas a `axe-accesibilidad.spec.ts` · capturas 1280×800 y 390×844 de la landing generadas y MIRADAS (`tests/e2e/screenshots/h12c-landing-{1280x800,390x844}.png`): hero con gradiente azul de marca, 5 secciones con captura real alternadas, banner de cookies visible, planes con insignia "propuesta", FAQ, CTA final, footer con enlaces legales — sin desbordamiento horizontal en 390px · compuertas: `npm run lint` 0 errores, `npm run typecheck` 0 errores, `npm run test:unit` 416/417 (+1 skip heredado), `npm run test:integration` 164/164, `npm run test:adversarial` 146/146 (+22 nuevas de H12c), `npm run build` (api+web) OK — logs `docs/logs/h12c-{lint,typecheck,test-unit,test-integration,test-adversarial,build}-20260906-121544.log` · decisiones expuestas: `docs/BLOQUEOS.md` D-006 (precios de lista de los 3 planes, propuesta pendiente de aprobación del fundador) · pendiente/declarado (no simulado): cablear `requireEntitlement()` en las rutas reales de creación de hotel/habitación/agente/mensaje (propiedad de H12a/H12b/otros agentes, función y prueba ya listas para consumir); CFDI real del SaaS (usa el mismo `DualPacCfdiPort` fake que el resto del sistema); credenciales reales de Stripe/Conekta Billing/PostHog/Sentry (ver README de `packages/mcp-servers/billing` y `packages/analytics`); dominio final de producción para `og:url`/canonical de la landing (depende de LAUNCH-010) · siguiente: merge junto con H12a/H12b cuando cierren; el fundador decide D-006 antes de anunciar precios en cualquier canal externo.
- H12c cierre (Sonnet) · verificación final tras el corte 429: capturas de la landing regeneradas y MIRADAS a ambos tamaños (1280×800 y 390×844, `tests/e2e/screenshots/h12c-landing-*.png` — hero con gradiente de marca, 5 secciones con captura real, sin scroll horizontal en 390px) mediante un preview de `apps/web` en puerto privado (el puerto 4173 por defecto de `playwright.config.ts` estuvo ocupado por el preview de otro worktree en paralelo — contención documentada, no un defecto de la suite); quitado `loading="lazy"` de las 5 capturas de sección (payload pequeño, evitaba una carrera de `IntersectionObserver` en la prueba E2E sin aportar beneficio real) y acotado el locator del footer en `tests/e2e/h12c-landing.spec.ts` (el banner de cookies también enlaza "aviso de privacidad" en su propio texto, a propósito). Compuertas relanzadas limpias: `npm run lint` 0 errores, `npm run typecheck` 0 errores, `npm run test:unit` 416/417 (+1 skip heredado), `npm run test:integration` 164/164, `npm run test:adversarial` 146/146, `npm run build` (api+web) OK — logs `docs/logs/h12c-{lint,typecheck,test-unit,test-integration,test-adversarial,build}-20260906-170121.log`. `docs/REQUISITOS.md` §3.17 y `docs/ACEPTACION.md` §3.17 con 15/15 `REQ-LAUNCH-*` coincidentes (`diff` vacío); `docs/BLOQUEOS.md` D-006 registrado (precios de lista de los 3 planes, decisión reservada al fundador). Sin fusionar a `main` (instrucción explícita del orquestador).
