# Corrección lote B — fiscal, agéntico, tool-calling (auditoría-2)

Alcance: `packages/domain-hotel`, `packages/agent-core`, `apps/api/src/routes/{folios,agentes,aprobaciones,mensajeria,cfdi}.ts` (más `apps/api/src/routes/{huespedes,mantenimiento,aprobacionesWhatsapp}.ts` y `apps/api/src/jobs/{noShow,nightAudit,nightAuditScheduler}.ts` por hallazgos que el orquestador amplió al lote durante la corrección), `apps/web/src/pages/Agentes.tsx`, migraciones `0070`–`0075` (rango asignado 0070–0079), `tests/**`. Método Likida: prueba-primero (se verificó que cada prueba nueva reproduce el hallazgo en rojo revirtiendo el fix, y pasa en verde con el fix restaurado — documentado en cada commit), un commit por hallazgo, sin tocar `packages/db` fuera de mi rango de migraciones ni `server.ts`/rutas de privacidad (propiedad del otro corrector en `main`).

Nota de entorno: la máquina de este worktree corre en paralelo con procesos de **otros proyectos ajenos** (`atiende-rentas-vacacionales-staging`, confirmado por `ps aux`), lo que agota memoria compartida de Postgres bajo alta paralelización (`shmget: No space left on device`) al correr la suite completa con muchos forks simultáneos. No es un defecto de este código: cada archivo de prueba pasa limpio de forma aislada o con paralelismo moderado (`--pool=forks --poolOptions.forks.maxForks=2` o `singleFork`, que es lo que usan `npm run test:integration`/`test:adversarial`).

## Tabla hallazgo → estado

| # | Hallazgo | Rubro | Severidad | Estado | Commit | Prueba |
|---|---|---|---|---|---|---|
| F1 | El impuesto que manda el cliente sobrescribía el cálculo fiscal determinista | fiscal | CRÍTICO | **Cerrado** | `ce0d031` | `tests/integration/folio/cargos-descuentos-reverso.spec.ts` (2 nuevas: 422 en fraude, 201 si coincide exacto) |
| F2 | ISH aplicado sobre A&B/extras/ajuste, fijado como correcto en un test | fiscal | CRÍTICO | **Cerrado** | `e32a51a` | `tests/unit/domain-hotel/folio-engine.spec.ts` (corregido + caso con tasa real de la fuente 5%) |
| F3 | CFDI de no-show declara un total distinto al que el folio cobró | fiscal | CRÍTICO | **Cerrado** | `1aa8542` | `tests/integration/contracts/cfdi/hospedaje.spec.ts` caso 5 (reescrito, verifica igualdad exacta folio↔CFDI) |
| ALTO-fiscal | `CfdiRelacionados` tipo 07 nunca llega al PAC | fiscal | ALTO | **Pendiente** | — | Requiere extender `TimbrarInput` en `packages/mcp-servers/cfdi` (fuera de mi rango de archivos; propiedad de otro módulo, ADR-007). Recomendado para lote C. |
| ALTO-fiscal | `CfdiPort` sin campo Forma de Pago (`c_FormaPago`) | fiscal | ALTO | **Pendiente** | — | Mismo motivo que el anterior — requiere tocar el contrato de `packages/mcp-servers/cfdi`, fuera de mi rango. Recomendado para lote C. |
| ALTO-fiscal | Calendario fiscal sin alerta real (solo tabla) | fiscal | ALTO | **Pendiente** | — | Requiere una ruta/job nuevo + integración con el canal de notificación (WhatsApp/Daily Flash) y arranque en `server.ts` (fuera de mi alcance: `server.ts` es del otro corrector en `main`). Recomendado para lote C, coordinando el arranque del job con el dueño de `server.ts`. |
| MEDIO-fiscal | Tasa de ISH sembrada por defecto (3%) no corresponde a la fuente (5% Quintana Roo) | fiscal | MEDIO | **Evaluado, no corregido** | — | Cambiar el default rompería las decenas de pruebas existentes que fijan `0.16/0.03` como fixture (incluidas las que F1–F3 ya tocan) — alto riesgo de regresión para un hallazgo MEDIO con el propio auditor admitiendo no haber confirmado que exista una ruta de alta de hotel real que dependa de este default fuera del seed. Recomendado: nueva migración con `state_code`→tasa real por estado en un hito dedicado, no un cambio puntual del default. |
| Rendimiento-CRÍTICO | `GET /reservas`/`/huespedes` registraban `dbSession` dos veces (colapso del pool bajo concurrencia) | rendimiento | CRÍTICO | **Cerrado** | `3967535` | `tests/integration/api/reservas-carga-concurrente.spec.ts` (nuevo: 50 concurrentes, 0 fallos, p95<500ms; reproducido en rojo revirtiendo el fix — ~44/50 fallos 500 a los 5s) |
| B1 | `POST /reservas/procesar-no-show` postea la penalización dos veces con doble clic | backend | CRÍTICO | **Cerrado** | `60e2c82` | `tests/integration/reservas/no-show-idempotente.spec.ts` (nuevo caso concurrente + migración 0070 `charge_no_show_reservation_idx`) |
| B2 | El lock de night audit no protege al planificador automático (solo al disparo manual) | backend | CRÍTICO | **Cerrado** | `8c30964` | `tests/integration/revenue/night-audit-scheduler.spec.ts` (nuevo: dos conexiones físicas reales, réplicas simuladas) |
| P1 | Resumen de caja del night audit agrupa por `created_at::date` crudo, no por fecha de negocio local | pruebas | ALTO | **Cerrado** | `72d16de` | `tests/integration/revenue/night-audit.spec.ts` (nuevo caso con huso de hotel distinto al de la sesión) |
| A3/T3/backend-ALTO | `PostgresApprovalQueue.decide()` sin lock de fila — dos decisiones concurrentes pueden ambas ejecutar | agéntico/tool-calling/backend | CRÍTICO/ALTO | **Cerrado** | `9f1d9f0` | `tests/unit/agent-core/postgres-approval-concurrency.spec.ts` (nuevo, código real + SqlClient fake determinista) — es también la prueba P2 pedida |
| A4 | Una aprobación "aprobada" se re-ejecuta (reintento del modelo o llamada concurrente) | agéntico/backend | ALTO | **Cerrado** | `6ac71aa` | `ApprovalQueue.markExecuted()` en la batería de contrato compartida + `tests/unit/agent-core/runner.spec.ts` (nuevo caso) |
| A2 | La ejecución diferida de una aprobación no recomprueba el gate vigente | agéntico | CRÍTICO | **Cerrado** | `78d5563` | `tests/integration/api/housekeeping-mantenimiento.spec.ts` (nuevo: bajar a shadow tras 1ª confirmación bloquea la ejecución) |
| T1 | `redact()` enmascara el teléfono destinatario en el texto del aprobador (queda ciego) | tool-calling | CRÍTICO | **Cerrado** | `75095ed` | `tests/unit/agent-core/{redact,runner}.spec.ts` (nuevo: últimos 4 dígitos visibles) |
| T2 | Plantilla transaccional se auto-aprueba sin verificar destinatario = huésped en curso | tool-calling | CRÍTICO | **Cerrado** | `8eca056` | `tests/integration/agent-core/journey-checkin-incidencia.spec.ts` (2 nuevos casos) |
| L-tarjeta | Número de tarjeta en WhatsApp se guarda en texto plano | legal | CRÍTICO | **Cerrado** | `7e1d7bc` | `tests/integration/api/mensajeria.spec.ts` + `tests/unit/domain-hotel/payment-free-text-guard.spec.ts` (nuevos, Luhn real) |
| A1 | "Demo (simulada)" ejecuta efectos reales fuera de gate shadow | agéntico | CRÍTICO | **Cerrado** | `5efcb0b` | `tests/integration/api/agentes.spec.ts` (nuevo: hotel en autopilot + demo no crea nada real) |
| A5/backend-MEDIO | Techo de costo mensual por agente sin lock (condición de carrera) | agéntico/backend | ALTO | **Cerrado** | `93558cf` | `tests/integration/api/agentes.spec.ts` (2 nuevos: mecanismo directo con 2 sesiones reales + invariante end-to-end) |
| backend-ALTO | `role` del aprobador se toma del body del cliente, no de la sesión | backend | ALTO | **Cerrado** | `5458399` | `tests/integration/api/housekeeping-mantenimiento.spec.ts` (nuevo: segundo owner no puede mentir su rol) |
| MEDIO-agéntico | `roleParamsForChannel()` nunca conectado al runtime real | agéntico | MEDIO | **Cerrado** | `7272220` | `tests/unit/agent-core/runner.spec.ts` (nuevo: effort llega al proveedor) |
| A6 | GOB-026 exige dos roles — imposible en hotel de un solo administrador | agéntico | ALTO | **Cerrado** | `13125e9` | `tests/integration/api/housekeeping-mantenimiento.spec.ts` (2 nuevos: delegado configurable, migración 0075) — de paso se corrigió un bug preexistente (500 siempre) en `PATCH /mantenimiento/:id/asignar` |
| backend-ALTO | Reclamación de idempotencia del botón de WhatsApp en transacción separada del efecto | backend | ALTO | **Cerrado** (solo `aprobacionesWhatsapp.ts`) | `97755a2` | `tests/integration/contracts/whatsapp/aprobaciones-boton.spec.ts` (nuevo: falla a medio camino → reclamación revertida, reintento sí procesa) |
| backend-ALTO | Mismo defecto en `mensajeria.ts` (webhook de mensajes entrantes) | backend | ALTO | **Evaluado, no corregido** | — | `mensajeria.ts` usa `engine.admin` (conexión única de proceso, sin pool) porque el mensaje entrante no tiene actor de staff que autentique una sesión RLS (`withAppSession`). Envolver varias sentencias en una transacción explícita sobre esa ÚNICA conexión compartida arriesga corromper otras peticiones concurrentes que la usan al mismo tiempo (a diferencia de `aprobacionesWhatsapp.ts`, que sí pudo moverse a `withAppSession` porque ya resuelve un actor real). Requiere agregar al motor un método de sesión "de sistema" respaldado por el POOL (bypassa RLS, sin usuario) — cambio de infraestructura fuera de este lote. Recomendado para lote C. |
| backend-ALTO | CFDI/pago llaman al proveedor externo antes del commit, idempotencia solo en memoria | backend | ALTO | **No abordado** | — | El propio informe original lo marca "latente, no explotable" (adaptadores reales son stubs sin credenciales, ADR-007); requiere patrón outbox para la llamada síncrona al PAC/PSP, cambio de arquitectura mayor. Recomendado para lote C cuando haya credenciales reales que hagan el riesgo explotable. |
| backend-ALTO | Verificación pública "código+apellido" degradada, rate-limit por IP evadible | backend | ALTO | **Fuera de alcance** | — | Vive en `apps/api/src/routes/cancelacionPublica.ts`/`middleware.ts` — no está en mi lista de archivos asignada (rutas públicas/privacidad quedaron reservadas al corrector de `main`). Reportado para el lote correspondiente. |

## Compuertas de calidad

- `npm run lint`: **0 errores** (1 warning preexistente ajeno en `tests/e2e/paridad-restaurantes-login.spec.ts`, no tocado en este lote) — `docs/logs/aud2-B-lint.log`.
- `npm run typecheck`: **0 errores** — `docs/logs/aud2-B-typecheck.log`.
- `npm run test:unit`: ver `docs/logs/aud2-B-test-unit.log`.
- `npm run test:integration`: ver `docs/logs/aud2-B-test-integration.log`.
- `npm run test:adversarial`: ver `docs/logs/aud2-B-test-adversarial.log`.
- `npm run build`: ver `docs/logs/aud2-B-build.log`.
- Cada hallazgo cerrado se verificó individualmente revirtiendo el fix (`git diff`/`git checkout`) y confirmando que la prueba nueva falla en rojo con el defecto original, luego se restauró el fix y se confirmó verde — documentado en el cuerpo de cada commit.
- No se tocó `packages/db` fuera de las migraciones `0070`–`0075`, ni `server.ts`, ni rutas de privacidad/públicas reservadas al otro corrector.
